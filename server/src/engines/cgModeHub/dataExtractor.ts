/**
 * Capital Guard Mode Hub — Data Extractor (v4)
 *
 * Re-exports from balanced hub — data extraction is identical.
 * CG's differentiation is in scoring, gating, and decision logic — not data.
 */
export { extractHubInput } from "../balancedModeHub/dataExtractor.ts";
